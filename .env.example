# .env.example - DO NOT PUT REAL SECRETS HERE
# Copy this to `.env` for local development and fill real values.

# MongoDB connection string (MongoDB Atlas)
MONGODB_URI=mongodb+srv://Agrilearn:<db_password>@agrilearn.c2m9dou.mongodb.net/?appName=AgriLearnretryWrites=true&w=majority

# JSON Web Token secret used to sign auth tokens
JWT_SECRET=replace_with_secure_random_string
JWT_EXPIRE=30d

# Server port (optional locally)
PORT=5000

# Frontend URL used in email verification and reset links
FRONTEND_URL=https://agri-learn-cacao-dz1udywbr-julius-guimtes-projects.vercel.app/

# SMTP / email settings (use app password for Gmail if 2FA enabled)
EMAIL_USER=your-email@example.com
EMAIL_PASSWORD=your-email-app-password

# Vite frontend build-time env (must be prefixed with VITE_)
# Example value should point to your deployed backend API (include '/api' if frontend expects it)
VITE_API_URL=https://your-backend.example.com/api
